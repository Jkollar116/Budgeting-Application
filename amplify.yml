version: 1
frontend:
  phases:
    preBuild:
      commands:
        - echo "No pre-build step needed"
    build:
      commands:
        - echo "No build step needed"
  artifacts:
    baseDirectory: src/main/resources
    files:
      - '**/*'
  cache:
    paths: []
